# Memory: architecture/decision-rules-column-audit-v1
Updated: 2026-01-20

## decision_rules Table Column Audit Report

### ✅ COLUMNS DROPPED (This Migration)

| Column | Reason | Violation |
|--------|--------|-----------|
| `response_en` | Language-specific farmer text | Language independence, SSOT |
| `response_hi` | Language-specific farmer text | Language independence, SSOT |
| `response_mr` | Language-specific farmer text | Language independence, SSOT |
| `trigger_keywords` | Language-dependent heuristics | Model replaceability |
| `action_text` | Duplicate narration responsibility | SSOT |

### ⚠️ COLUMNS FLAGGED FOR FUTURE REVIEW

| Column | Status | Justification |
|--------|--------|---------------|
| `alternatives` (JSONB) | KEEP | Used by symbolic reasoner for product substitution |
| `observable_characteristics` (JSONB) | KEEP | Structured data for differential diagnosis |
| `differentiating_questions` (JSONB) | KEEP | DB-driven clarification generation |
| `visual_markers` (JSONB) | KEEP | Supports image-based diagnosis |
| `icar_package` | KEEP | Scientific source tracking |
| `scientific_basis` | KEEP | Audit trail for recommendations |
| `weather_dependency` (JSONB) | KEEP | Used by symbolic reasoner for application timing |

### ✅ CORE SYMBOLIC COLUMNS (MUST KEEP)

These columns are essential for the symbolic decision brain:

- `rule_id` - Primary identifier
- `crop_group`, `crop_code` - Crop targeting
- `category`, `canonical_group` - Rule classification
- `stage_applicable` - Growth stage filtering
- `conditions_json` - Symbolic matching conditions
- `condition_code` - Rule trigger code
- `cause` - Identified problem/cause
- `priority` - Rule precedence
- `action_type` - Treatment/advisory classification
- `is_active` - Rule activation status
- `version` - Rule versioning

### ✅ TREATMENT METADATA COLUMNS (MUST KEEP)

Required for agronomically safe recommendations:

- `phi_days` - Pre-harvest interval (safety)
- `active_ingredient` - Chemical identification
- `organic_alternative` - Organic options
- `ipm_level` - IPM hierarchy positioning
- `bee_toxicity` - Pollinator safety
- `etl_threshold` - Economic threshold level

### 🔒 ARCHITECTURAL GUARANTEES

After this cleanup:

1. **Language Independence**: No farmer-facing text in decision_rules
2. **SSOT Preserved**: Symbolic brain remains sole decision authority
3. **Narration Separation**: All text generated by LLM layer using i18n_keys
4. **Scalability**: Adding new languages requires ZERO schema changes
5. **Model Replaceability**: No keyword heuristics in symbolic core
