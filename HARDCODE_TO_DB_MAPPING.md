# HARDCODE_TO_DB_MAPPING

**Date:** 2026-07-05  
**Companion to:** `DATABASE_ONTOLOGY_AUDIT.md`  
**Legend:** **REUSE** = column already exists and is (or should be) populated · **EXTEND** = add column to an existing table · **NEW TABLE** = requires justification, deferred to `MISSING_DB_CAPABILITY_REPORT.md`.

---

## Verdict Table

| # | TS Constant | Location | Agronomic Meaning | DB Owner (existing) | Verdict | Notes |
|---|---|---|---|---|---|---|
| 1 | `CROP_GDD_CONFIG`, `WHEAT_PHENOLOGY`, `RICE_PHENOLOGY`, `COTTON_PHENOLOGY`, `SUGARCANE_PHENOLOGY` | `agents/gdd-phenology-engine.ts:84–467` | GDD base temp per crop + per-stage GDD accumulation thresholds w/ Zadoks/BBCH codes | `crop_stage_master.base_temperature_c`, `.gdd_min`, `.gdd_max`, `.phenology_model`, `.ontology_id`; variety overrides in `variety_phenology_profile.gdd_target` | **REUSE (populate)** | Columns already exist. 0/221 rows populated → seed, don't redesign. |
| 2 | `STAGE_FAMILIES`, `STAGE_SYNONYMS` | `layered-rule-evaluator.ts` (emptied), `clarification-contract.ts` (emptied) | Stage equivalence graph | `crop_stage_master` (parent_stage_id, prev_stage_id, next_stage_id, canonical_stage_id) + `crop_stage_aliases` + `crop_stage_graph` | **REUSE** | Already deleted from TS; ensure aliases seeded (195 rows currently for 11 crops). |
| 3 | `CROP_PHOTOPERIOD_PROFILES` + inline DAS windows | `agents/photoperiod-calculator.ts:50–341` | Crops sensitive to day length + critical hours + DAS window | `crop_stage_master.is_photoperiod_sensitive` (**28 rows true — column EXISTS**) + `crop_stage_master.das_min/max` for the window | **REUSE + EXTEND (add critical_day_length_hours)** | Boolean already there; only numeric hours needs adding. |
| 4 | `POLLINATOR_DEPENDENT_CROPS` (flowering DAS windows) | `agents/pollinator-protection-rules.ts:274–360` | Which crops depend on pollinators + flowering window | `crop_stage_master` WHERE `growth_stage='FLOWERING'` gives DAS window per crop | **REUSE** | Derive at runtime; no new column needed. |
| 5 | `YOUNG_CROP_MAX_DAYS` (×2 duplicates) | `decision/unified-decision-gate.ts:303`, `decision/prescription-gate-enforcer.ts:139` | Earliest DAS at which chemical spray is biologically safe | `crop_stage_master` | **EXTEND** — add `chemical_safe_from_das INTEGER` | Alternative: derive from lowest non-nursery stage `das_min`. Prefer explicit column for auditability. |
| 6 | `CROP_IRRIGATION_PROFILES` + system-efficiency factors | `agents/irrigation-decision-module.ts:71,241` | Per-crop per-stage mm/day + irrigation interval + drip/sprinkler/furrow efficiency | `crop_baseline_guidelines_v2.water_requirement_mm`, `.irrigation_interval_days`, `.critical_moisture_percent` for water; `irrigation_types` for system-efficiency | **REUSE + EXTEND** | Add `irrigation_types.efficiency_factor NUMERIC`. Delete sugarcane-default fallback. |
| 7 | `NITROGEN_REQUIREMENTS_BY_STAGE`, `PHOSPHORUS_REQUIREMENTS`, `POTASSIUM_REQUIREMENTS` | `agents/soil-ndvi-state-calculator.ts:31–168` | Per-crop per-stage NPK requirement bands | `crop_baseline_guidelines_v2.nitrogen_min/max/optimal`, `.phosphorus_*`, `.potassium_*` | **REUSE (populate)** | 97 rows covering 11 crops; extend row coverage. |
| 8 | `NDVI_THRESHOLDS_BY_CROP`, `EXPECTED_NDVI_BY_STAGE`, NDVI trend cutoffs | `soil-ndvi-state-calculator.ts:31–299` | Per-stage healthy NDVI band | `crop_stage_master.expected_ndvi_min`, `.expected_ndvi_max` (0/221 populated) + `variety_phenology_profile.expected_ndvi_*` | **REUSE (populate)** | Trend cutoffs (0.02/0.05) go to `system_config`. |
| 9 | `PHI_DATABASE` (20+ chemicals w/ domestic + EU + US PHI, MRL FSSAI, MRL EU, crop-specific PHI) | `agents/phi-enforcement-guardian.ts:67–359` | Pre-harvest interval + max residue limits per active-ingredient | Product-level PHI: `master_products.pre_harvest_interval_days` (67/196 populated); regulatory chemical-level: `chemical_regulatory_status` (thin) | **EXTEND `chemical_regulatory_status`** — add `active_ingredient`, `phi_days_domestic`, `phi_days_export_eu`, `phi_days_export_us`, `mrl_fssai_ppm`, `mrl_eu_ppm`, `who_toxicity_class`, `max_dose_g_per_ha`, `crop_specific_phi jsonb` | Do NOT create parallel table. |
| 10 | `NEAR_HARVEST_ALTERNATIVES` (bio-alternatives w/ target pest + application method) | `phi-enforcement-guardian.ts:365–429` | List of PHI=0 bio-alternatives per target pest | `decision_rules` WHERE `ipm_level<=4 AND pre_harvest_interval_days=0` joined `master_products` | **REUSE** | Runtime query, not new table. |
| 11 | `MAX_SAFE_DOSES` | `deterministic-response-builder.ts:301` | Max dose per hectare per active-ingredient | `chemical_regulatory_status.max_dose_g_per_ha` (to be added — see row 9) | **EXTEND same as row 9** | Single migration covers both. |
| 12 | `WHO_TOXICITY_CLASSES` + inline `getWHOToxicityClass()` substring lookup | `safety-guardian.ts:753` | WHO toxicity class per active-ingredient | `chemical_regulatory_status.who_toxicity_class` (to be added) | **EXTEND** | Kill `.includes()` classifier. |
| 13 | `BANNED_CHEMICALS` (14) + `BANNED_SUBSTANCES_INDIA` (parallel list) | `response-validation-gate.ts:45`, `safety-guardian-types.ts` | Regulator-banned actives | `chemical_regulatory_status.status='BANNED'` (**59 rows already**) | **REUSE** | Delete both TS lists. |
| 14 | `POLLINATOR_TOXICITY_DB` (LD50 contact/oral, residual days, flowering ban, evening spray allowed) | `pollinator-protection-rules.ts:68–268` | Bee toxicity per active-ingredient | `chemical_regulatory_status` (add pollinator columns) | **EXTEND** — add `pollinator_bee_ld50_contact_ug`, `pollinator_bee_ld50_oral_ug`, `pollinator_residual_toxicity_days`, `pollinator_flowering_banned bool`, `pollinator_evening_spray_allowed bool` | Same table as row 9. |
| 15 | Inline neonic/aquatic lists + buffer distances (50m / 100m) | `safety-guardian.ts:284–323` | Buffer zone distances | `chemical_regulatory_status` | **EXTEND** — add `buffer_pollinator_m`, `buffer_aquatic_m` | Consolidate with row 14. |
| 16 | `SPRAY_LIMITS` + `SPRAY_THRESHOLDS` (product-type weather envelope: wind, rain, temp, humidity, dry hours) | `agents/spray-window-calculator.ts:63`, `decision/weather-safety-gate.ts:96` | Per product-type spray weather safety | **No existing owner.** `disease_risk_model` covers disease risk; `master_products.weather_conditions jsonb` is per-SKU not per-type. | **NEW TABLE justified** — `spray_condition_thresholds(product_type, wind_max_kmph, rain_prob_max_pct, temp_min_c, temp_max_c, humidity_min_pct, humidity_max_pct, dry_hours_required_min)` — see MISSING report. |
| 17 | Disease-risk scoring constants (humidity 85/70, dew-point 2/5°C, temp 20–28°C, rainfall 15/5mm, score 75/50/25) | `weather-safety-gate.ts:162–222` | Environmental disease pressure scoring | `disease_risk_model` — thresholds are per-disease and already live here | **REUSE** | Delete inline generic scoring; use per-disease rows. |
| 18 | `EMERGENCY_OBS_CODES` inline Set | orchestrator/rule-evaluator | Codes triggering emergency bypass | `emergency_observation_codes` (**38 rows already**) | **REUSE** | Delete TS Set; query DB. |
| 19 | Critical-stage inline list | multiple gates | Stages where any error is high-cost | `crop_stage_master` | **EXTEND** — add `is_critical_stage BOOLEAN DEFAULT FALSE` | Sparse boolean; audit-friendly. |
| 20 | `IPM_LABELS` (int→English), `IPM_URGENCY_LABELS` (string→English) | `deterministic-response-builder.ts:368`, `llm-response-formatter.ts:176` | Farmer-facing labels for IPM levels | i18n bundles (frontend), not agronomy | **REUSE — no DB needed** | Move to `src/i18n/locales/*/decision.json`. |
| 21 | `INTENT_SCOPE_MAP` (160 lines) | orchestrator | Per-intent allowed observation groups + context requirements | `observation_intent_master.allowed_observation_groups`, `.requires_crop_context`, `.requires_stage_context`, `.routing_target`, `.clarification_mode` (**90 rows, 25 categories**) | **REUSE** | All columns present; delete TS map. |
| 22 | `DEFAULT_CONFIDENCE_RULES` (Bayesian weights, thresholds, decay rate) | `hypothesis-types.ts:268` | Engine tuning constants | `system_config` (generic jsonb key-value) | **REUSE** — insert row `config_key='hypothesis_engine_defaults'` | Not agronomy → no ontology table. |
| 23 | `CAUSE_NAMES`, `EXPLANATIONS`, `getConfirmingEvidence()` string bank | `decision/diagnostic-escalation-generator.ts:95–369` | Cause labels + biological explanations | `hypothesis_master.cause_name_en/mr/hi`, `.biological_basis` (**346 rows**) | **REUSE** | Delete TS bank; look up by hypothesis_id. |
| 24 | `CATEGORY_PATTERNS`, `PLANT_PART_PATTERNS` (keyword-inference) | `layered-rule-evaluator.ts` (emptied), `diagnostic-escalation-generator.ts:315` | Category / plant-part inference from code substrings | `observation_master.observation_category` (2195/2540 populated), `.affected_plant_part` (2215/2540 populated) | **REUSE (backfill remaining rows)** | Delete `.includes()` classifiers permanently. |
| 25 | Cost defaults (₹/acre by product type + IPM level) | `agents/economic-calculator.ts:117–141` | Economic estimate benchmarks | `crop_baseline_guidelines_v2` (no cost columns yet) OR `master_products.price_per_unit`+dose | **REUSE `master_products`** for product-specific; **EXTEND `crop_baseline_guidelines_v2`** with `input_cost_inr_per_acre_low/mid/high` for stage-level benchmarks. | Prefer product-level. |
| 26 | `TREATMENT_ACTIONS` sets (×4 files) | multiple | Set of action_types considered "treatment" | `decision_rules.action_type` (SSOT) | **REUSE — derive at runtime** | Delete all 4 duplicated sets. |
| 27 | `VAGUE_SYMPTOM_PATTERNS` (×2 files) | `decision-readiness-gate.ts`, `unified-decision-gate.ts` | Symptoms too vague for direct diagnosis | `observation_master.is_diagnostic=false AND clarity_score<X` (columns exist) | **REUSE** | DB-driven. |

---

## Summary

- **REUSE (columns already exist)**: 15 constants.
- **REUSE + populate (rows sparse)**: 4 constants (rows 1, 7, 8, 24).
- **EXTEND (add columns to existing tables)**: 7 constants — all fit on `chemical_regulatory_status`, `crop_stage_master`, `crop_baseline_guidelines_v2`, `irrigation_types`.
- **NEW TABLE justified**: **1** (`spray_condition_thresholds`).
- **Not DB (i18n / runtime config)**: 2 constants (rows 20, 22).

Prior refactor plan proposed **9 new tables**. Audit reduces this to **1 new table**.
